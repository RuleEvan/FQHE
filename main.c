#include "hilbert.h"

int main(int argc, char *argv[]) {
//  generate_interaction_file(4); 
//  compute_1body_energy(3,6,0);
//  gsl_complex u = u_spin_l(1.5, 1.5, M_PI/8, 1.0);
//  printf("%g, %g\n", GSL_REAL(u), GSL_IMAG(u));
  hierarchy(0.5, 0.5, 4);
  return 0;
}
